# RISK SETTINGS LOCK
# DO NOT MODIFY - These are proven conservative settings
# Last verified: November 20, 2025

[real_risk]
max_risk_per_trade = 0.012      # 1.2% per trade
daily_loss_limit = 0.04         # 4% daily max loss
max_any_strategy = 0.35         # 35% max allocation per strategy
max_positions = 20              # Conservative position count
per_trade_capital = 0.01        # 1% capital per trade

[proven_settings]
# These settings have been backtested and work
min_signal_strength = 75        # Don't lower this
max_trades_per_hour = 100       # Don't increase aggressively
circuit_breaker_loss = 0.03     # Emergency stop at 3%

[notes]
why_conservative = "Consistent 2-4% daily returns compound to 500-1000% annually"
dont_chase = "25% daily targets lead to overleveraging and blowups"
proven_record = "System has 78.6% win rate with conservative settings"
